mkdir ( nombre del folder ) = crear un directorio ( folder )

cd (con el nombre del folder ) = es bucar en el disco la carpeta

npm init -y = creacion del package de json y (-y es dar si todos los permisos)

npm install express mysql body-parser express-session = instalacion de la libre npm al igual q mysql  con la instalaccion de express con el protocolo 

npm install -g npm@(version ) = instalacion y actualizacion de la version q le pide 

code . = Entrar al directorio 


(intslacion de q si no tiene un archivo en modules_node hacer el siguiente codigo ) = npm install ( nombre del modulo  q le falta ) 

express , mysql bodyParser , express -session y ejs ) = importante en modulos de node si no no va a funcionar. 